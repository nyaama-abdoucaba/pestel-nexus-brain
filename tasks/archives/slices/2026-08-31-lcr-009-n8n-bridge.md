# LCR-009 — Pont n8n vers le runtime Python

**Spec** : `2026-08-31-linkedin-comment-runtime-spec.md`  
**Statut** : prêt pour test manuel d’intégration  
**Effort** : S

## Livré

Le workflow principal `LinkedIn Apify to Ollama Comments` (`Odr7h7W64hcOhbgS`)
appelle directement `POST http://host.docker.internal:8000/v1/linkedin-comment-runs`
avec l’identifiant du post upserté et `reader_world: "icp1"`, sans budget. Son
digest lit le contrat `CommentRunResult` v2 (`outcome`: `draft`, `skip`, `error`).

## Reste à faire avant bascule

1. Appliquer la migration sur la base PostgreSQL existante.
2. Démarrer FastAPI sur le port 8000 vu par n8n.
3. Lancer un échantillon manuel et vérifier le digest pour les trois outcomes,
   sans publier le workflow.
